cli_requirement: ">=0.41.0"

supported_targets: '*'

default_target: "qemux86-64"

distro:
  release: 2024
  channel: edge

runtimes:
  dev:
    packages:
      avocado-runtime: '*'
    extensions:
      - example-reactjs
      - avocado-ext-dev
      - avocado-ext-sshd-dev
      - avocado-ext-webkit
      - avocado-bsp-{{ avocado.target.board }}

    # QEMU targets: no display hardware, drop webkit/cog
    qemux86-64:
      extensions:
        - example-reactjs
        - avocado-ext-dev
        - avocado-ext-sshd-dev
        - avocado-bsp-{{ avocado.target.board }}
    qemuarm64:
      extensions:
        - example-reactjs
        - avocado-ext-dev
        - avocado-ext-sshd-dev
        - avocado-bsp-{{ avocado.target.board }}

extensions:
  avocado-ext-dev:
    source:
      type: package
      version: '*'

  avocado-ext-sshd-dev:
    source:
      type: package
      version: '*'

  avocado-ext-webkit:
    source:
      type: package
      version: '*'

  avocado-bsp-{{ avocado.target.board }}:
    source:
      type: package
      version: '*'

  example-reactjs:
    version: 0.1.0

    overlay: overlay

    enable_services:
      - ref-reactjs.service

    on_merge:
      - systemctl start --no-block ref-reactjs.service
      - sh -c 'command -v cog >/dev/null 2>&1 && systemctl restart --no-block cog.service || true'

    on_unmerge:
      - systemctl stop ref-reactjs.service
      - sh -c 'systemctl stop cog.service 2>/dev/null || true'

    packages:
      example-reactjs-app:
        compile: example-reactjs-app
        install: reactjs-install.sh
      nodejs: '*'

    sdk:
      packages:
        nativesdk-nodejs: '*'
        nativesdk-nodejs-npm: '*'
        nativesdk-ca-certificates: '*'

rootfs:
  permissions: dev

initramfs:
  permissions: dev

# NOT FOR PRODUCTION: the `dev` profile sets an empty root password and is
# referenced by rootfs/initramfs above. Define additional profiles with real
# users, groups, and hashed passwords, then point rootfs/initramfs at them via
# `permissions: <name>`.
permissions:
  dev:
    users:
      root:
        password: ""

sdk:
  image: "docker.io/avocadolinux/sdk:{{ config.distro.release }}"

  container_args:
    - --privileged
    - --network=host
    - -v /dev:/dev
    - -v /sys:/sys

  compile:
    example-reactjs-app:
      compile: reactjs-compile.sh
      clean: reactjs-clean.sh

  packages:
    avocado-sdk-toolchain: '*'
