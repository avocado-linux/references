# Guest B. Reads each publish back and answers in its own half of the region.
VM_ROLE=pong
VM_SMP=1
VM_MEM=512M
# Unused by the responder -- it runs until stopped -- but kept so both env
# files have the same shape and launch-vm can source either one unchanged.
VM_ITERS=0
VM_PAYLOAD=4096
