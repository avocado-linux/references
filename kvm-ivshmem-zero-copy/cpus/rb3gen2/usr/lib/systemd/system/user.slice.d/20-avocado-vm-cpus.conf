[Slice]
# Keep general system work off the VM cores. cpu7 is included here on purpose:
# it is the core the unmovable managed IRQs land on, so it is the right place
# for housekeeping rather than for a latency-sensitive guest.
AllowedCPUs=0-3,7
